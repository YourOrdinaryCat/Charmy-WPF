<Window x:Class="Charmy.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:Charmy.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel}"
        Background="{DynamicResource WindowBackgroundBrush}"
        Title="Settings" MinHeight="320" MinWidth="500">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition MaxWidth="210" Width="1*" />
            <ColumnDefinition MaxWidth="210" Width="1*" />
            <ColumnDefinition MaxWidth="210" Width="1*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="32" />
            <RowDefinition Height="36" />
        </Grid.RowDefinitions>

        <TextBlock Text="Hot corner delay" />
        <TextBox x:Name="DelayBox"
                 Grid.Row="1"
                 Text="{Binding HotDelay, Mode=OneWay}" />
        <Button Grid.Row="2"
                Click="Button_Click"
                Content="Apply"
                Margin="0,4" />
    </Grid>
</Window>
